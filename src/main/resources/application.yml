spring:
  application:
    name: spring
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3307/qna_db?serverTimezone=UTC
    username: root
    password: watoo
  jpa:
    database: mysql
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQLDialect
logging:
  level:
    org:
      hibernate:
        SQL: debug